import React from 'react';
import styles from './BlurryFilter.module.scss';

const BlurryFilter = () => {
  return (
    <div className={styles.blurryFilter}></div>
  );
};

export default BlurryFilter;